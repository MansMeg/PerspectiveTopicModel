# Native R Travis Github
language: r
sudo: required
compiler: clang
cache: packages

# Be strict when checking our package
warnings_are_errors: true

notifications:
  email:
    on_success: change
    on_failure: change
